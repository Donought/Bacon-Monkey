class Spawner {
    constructor(){
        this.count = 0;
    }

    Spawner(){
        while (this.count >= 2){
            ball = new ball(x,y,d)
            this.count = this.count + 1;
        
        }
    }
}